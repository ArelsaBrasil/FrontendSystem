export type TChosenMarker = {
  osId: number;
  pointId?: number;
  groupName: string;
};

export type TGroup = {
  groupName: string;
  car: string;
  userName: string[];
};

export type TPointId = {
  pointId: number;
};
