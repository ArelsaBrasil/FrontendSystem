import { Home } from "../layouts/luzes/Home";

export function LuzesHome() {
  // const { user } = useContext(AuthContext);

  // useEffect(() => {
  //   api.get("/profile");
  // });

  return (
    <>
      <Home />
    </>
  );
}
